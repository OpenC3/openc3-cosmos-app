/target
/dist
/python
/cosmos
/bin
/tmp
Cargo.lock.bak
/openc3-app-settings.json
# Single-instance advisory lock, created at runtime (dev root is the cwd).
/openc3-app.lock
# Per-machine runtime state: bridge/identity.key is a private key (secret) and
# current.json holds a per-run Iroh ticket. Never commit these.
/bridge
# Plugin files synced from COSMOS to the host at runtime (regenerated per run).
/host_files
# Leftover samply profiler dumps.
/profile.json.gz
*.profile.json.gz
# Host-microservice working dirs, created at runtime.
/microservices
# macOS Finder metadata
.DS_Store
